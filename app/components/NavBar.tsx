import { useState } from "react";

interface NavBarArgs {
    navItems: string[];
}

export default function NavBar({ navItems }: NavBarArgs) {


    return (
        <div className="flex flex-col min-h-screen">
        </div>
    );
}

